<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{burger_name}}</h5>
	<button class="changeButton btn btn-primary" data-id="{{id}}" data-dev="{{devoured}}">
		{{#if devoured}}DEVOUR!{{else}}RE ORDER!{{/if}}
	</button>
  </div>
</div>